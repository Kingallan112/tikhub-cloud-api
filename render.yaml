services:
  # Web Service (Cloud API)
  - type: web
    name: tikhub-cloud-api
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
